# 📖 Библия за год — Telegram-бот

Бот помогает читать всю Библию за один год, ежедневно предлагая удобные порции чтения, ведёт учёт прогресса и мотивирует не пропускать дни.

---

## 🚀 Быстрый старт

1. **Склонируйте репозиторий:**
   ```bash
   git clone https://github.com/pasha-zigzag/bibleyear.git
   cd bibleyear
   ```

2. **Установите зависимости:**
   ```bash
   npm i
   ```

3. **Создайте файл `.env` и укажите токен вашего бота:**
   ```
   TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
   ```

4. **Запустите бота:**
   ```bash
   npm start
   ```
   Для режима отладки:
   ```bash
   DEBUG=telegraf:* npm run dev
   ```

---

## 🧐 Что умеет бот

- 📅 **Ежедневные главы** — бот сам подбирает главы на каждый день и показывает их в удобном формате.
- 📚 **Постраничное чтение** — стихи разбиты на страницы по 5 (или меньше) стихов, всегда только из одной главы.
- 📈 **Прогресс** — отмечает, когда вы завершили чтение дня.

---

## ⚙️ Настройка

- Все данные для чтения по дням лежат в папке `data/` — для каждого дня отдельный JSON-файл с номерами глав и стихов.
- Логика разбиения на страницы исключает ситуации, когда на одной странице смешиваются стихи из разных глав.
- В интерфейсе показывается текущий день, главы и номер страницы.

---

## 🛠 Используемые технологии

- [Node.js](https://nodejs.org/)
- [Telegraf](https://telegraf.js.org/) — фреймворк для Telegram-бота
- [dotenv](https://www.npmjs.com/package/dotenv) — для хранения токена

---

## 💡 Пример использования

**/start**
- Получаете приветствие, список глав на сегодня и кнопку "Начать чтение".

**Постраничная навигация**
- Листайте стихи кнопками "Вперёд" и "Назад", на каждой странице отображается только одна глава.

**Завершение чтения**
- После последней страницы появляется кнопка "Готово" и поздравление с завершением дня.

---

## 👨‍💻 Для разработчиков

- Основной код в `index.js`
- Для запуска с отладкой:  
  `DEBUG=telegraf:* npm run dev`
- Для автоперезапуска используйте [pm2](https://pm2.keymetrics.io/):
  ```bash
  pm2 start index.js --name bible-bot
  pm2 restart bible-bot
  ```

---

## 🕊 Пусть чтение приносит радость и пользу!  